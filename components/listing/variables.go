package listing

type variant string

const (
	Default   variant = ""
	Primary   variant = "uk-list-primary"   // Color
	Secondary variant = "uk-list-secondary" // Color
	Muted     variant = "uk-list-muted"     // Color
	Stripped  variant = "uk-list-striped"   //striped layout
	Divider   variant = "uk-list-divider"   //divider layout
)
